import SwiftUI

/// Shared constants for the dive overview **`.sheet`** (map + tank tabs).
enum DiveActivityOverviewPanelChrome {
    static let topCornerRadius: CGFloat = 20
    /// Frosted sheet fill — map / tank hero remains slightly visible through the panel.
    static let sheetBackgroundOpacity: CGFloat = 0.86
}
