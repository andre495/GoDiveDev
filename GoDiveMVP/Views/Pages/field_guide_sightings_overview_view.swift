import SwiftUI

/// Field Guide **My Sightings** — placeholder until the overview is rebuilt.
struct FieldGuideSightingsOverviewView: View {
    var body: some View {
        Color.clear
            .frame(maxWidth: .infinity, maxHeight: .infinity)
            .accessibilityIdentifier("FieldGuide.Sightings.Overview")
            .accessibilityLabel("My Sightings")
    }
}
