---
description: Run xcodebuild/tests only when the user asks to test, or before commit/push.
alwaysApply: true
---

# Xcode build / test — only when asked

**Do not** run **`xcodebuild`** (`clean`, `build`, `build-for-testing`, or `test`) proactively after code changes, feature work, or bug fixes — even when you added or updated tests.

## When to run

Run the Xcode workflow (**`.cursor/rules/xcode-run-test.mdc`**) only when:

1. **Explicit test / build request** — the user asks to **run**, **launch**, **build**, **test**, **⌘U**, verify compile, or similar. **Run on Dre's phone** uses **`.cursor/rules/xcode-run-dres-phone.mdc`** (clean → rebuild → install + launch).
2. **Commit and/or push** — the user asks to **`git commit`**, **`git push`**, or ship / commit-and-push. **Before** committing:
   - Run **`clean build-for-testing`** on the iPhone 17 simulator (default from **`xcode-run-test.mdc`**).
   - Fix compile errors, regressions you introduced, and **all project build warnings** (**`xcode-fix-build-warnings.mdc`**).
   - Run **`test`** only if the user also asked to run tests (or said “test” as part of the commit/push request).

## When not to run

- After implementing or fixing something, unless one of the triggers above applies.
- To “verify” or “confirm” your own work at handoff — tell the user the change is ready; they can ask to test.
- Because **`code-changes-require-tests.mdc`** asked you to **author** tests — writing tests does **not** require executing **`xcodebuild`**.

## Handoff without a build

When finishing work without a test request, skip build logs and do not claim **BUILD SUCCEEDED** / **FAILED**.
