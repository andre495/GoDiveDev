---
description: Hybrid trust boundaries — CloudKit dive log vs Firebase social/friends/CDN
alwaysApply: true
---

# GoDive hybrid trust boundaries

Source: **`GoDiveMVP/cursor/owasp_access_control_policy.md`**, **`hybrid_cloud_sync_boundaries.md`**.

## Ownership of data

| Data | Where it lives | Cloud |
|------|----------------|-------|
| Dive log (dives, buddies, trips, gear, certs, media **pointers**/previews, user sites/species) | SwiftData **user** store | **Private CloudKit** only (source of truth) |
| Full photo/video bytes | Photos / iCloud Photos | **Never** CloudKit / Firebase by default |
| Social directory (display name, interests, avatar URL) | Firestore `users/{uid}` (+ Storage avatar) | **Firebase** only |
| Friend graph + invites | Firestore `friendships`, `friendInvites` | **Firebase** only |
| Friend-visible dive **projections** (structured fields; notes/media opt-in) | Firestore `users/{uid}/sharedDives` (+ opt-in Storage previews) | **Firebase** — friends-only read; not a second dive account |
| App catalogs (Marine Life, ODM refs) | SwiftData **catalog** store + CDN | Firebase **Hosting/Storage catalog** — not user private DB |

## Do

- Keep dive ownership on **`ownerProfileID` → `UserProfile`** (Apple / CloudKit). **Never** use Firebase UID as dive-log owner.
- Soft-fail Firebase Auth/social when unconfigured; dive log must still work via SIWA + local/CloudKit.
- Fail closed on CDN checksum / non-HTTPS catalog base URL.
- When changing **`catalog-cdn/*.rules`**, deploy per **`firebase-rules-deploy.mdc`**.
- Friend share: mirror **projections** only when sharing is on and friendships exist; omit notes/media unless Settings opt-in.

## Do not

- Write FIT/UDDF originals or full Photos library bytes to **Firestore** / **Firebase Storage**.
- Put full media or import source files in **CloudKit** by default.
- Add buddy-to-buddy **co-editing** of one dive record without an explicit **CloudKit Sharing** design (friends get **read-only** projections).
- Broaden Firestore/Storage access in client code when rules are missing — fail soft on social features instead.
- Widen Firestore reads/writes beyond the documented matrix without updating **`owasp_access_control_policy.md`** and deploying rules.
